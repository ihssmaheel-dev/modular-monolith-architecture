title = "Modular Monolith Architecture secret scanning"

[extend]
useDefault = true

[[allowlists]]
description = "Generated and dependency output is not source-controlled input"
paths = [
  '''(^|/)(node_modules|dist|coverage|\.turbo)(/|$)''',
]
