{
  "name": "monorepo",
  "private": true,
  "scripts": {
    "bootstrap": "node scripts/bootstrap.js",
    "dev": "turbo dev",
    "dev:api": "pnpm --filter shared build && pnpm --filter api dev",
    "dev:api:debug": "pnpm --filter shared build && pnpm --filter api dev:debug",
    "dev:web": "pnpm --filter shared build && pnpm --filter web dev",
    "dev:mobile": "pnpm --filter shared build && pnpm --filter mobile dev",
    "build": "turbo build",
    "build:api": "pnpm --filter api build",
    "build:web": "pnpm --filter web build",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "test": "turbo test",
    "test:unit": "turbo test:unit",
    "test:api:watch": "pnpm --filter api test:watch",
    "test:integration": "turbo test:integration",
    "test:e2e": "turbo test:e2e",
    "db:migrate": "pnpm --filter api db:migrate",
    "db:migrate:status": "pnpm --filter api db:migrate:status",
    "db:migrate:down": "pnpm --filter api db:migrate:down",
    "db:migrate:dev": "pnpm --filter api db:migrate:dev",
    "db:seed": "pnpm --filter api db:seed",
    "docker:up": "docker compose -f docker/docker-compose.yml up -d --wait mongodb redis minio mailpit",
    "docker:init": "docker compose -f docker/docker-compose.yml run --rm minio-init",
    "docker:down": "docker compose -f docker/docker-compose.yml down",
    "docker:prod": "docker compose -f docker/docker-compose.prod.yml up -d --wait",
    "clean": "turbo clean && rm -rf node_modules",
    "prepare": "husky",
    "generate:module": "node scripts/generate-module.js",
    "architecture:check": "depcruise --config .dependency-cruiser.cjs apps/api/src --output-type err",
    "rules:check": "pnpm architecture:check && node scripts/check-rules.js"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "dependency-cruiser": "18.1.1",
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0",
    "prettier": "^3.3.0",
    "turbo": "^2.10.9",
    "typescript": "^7.0.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "lint-staged": {
    "apps/api/src/**/*.ts": [
      "pnpm --filter api lint"
    ],
    "*.{ts,tsx,js,jsx,json,md}": [
      "prettier --write"
    ]
  }
}
